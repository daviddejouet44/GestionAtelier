using Microsoft.AspNetCore.Builder;

namespace GestionAtelier.Endpoints.Settings;

public static class AccountsEndpoints
{
    public static void MapAccountsEndpoints(this WebApplication app, string recyclePath)
    {
        // User/account management endpoints are handled by AuthEndpoints
    }
}
